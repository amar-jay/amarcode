{
  "name": "acp-workbench",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun run --cwd apps/application dev",
    "build": "bun run --cwd apps/application build",
    "test": "bun run --cwd apps/application test",
    "tauri": "bun run --cwd apps/application tauri",
    "daemon": "cargo run -p acp-workbench-daemon",
    "daemon:cli": "cargo run -p acp-workbench-daemon --bin daemon-test-cli --",
    "daemon:check": "cargo test -p acp-workbench-daemon"
  }
}
